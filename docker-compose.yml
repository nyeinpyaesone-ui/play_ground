services:
  studio:
    build: .
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      WORKSPACE_DIR: /app/workspace
    volumes:
      - ./workspace:/app/workspace
    restart: unless-stopped
